# Volatile local Beads runtime artifacts
beads.db*
bd.sock
daemon.lock
daemon.log
daemon.pid

# Local-only interaction logs
interactions.jsonl

# Local state
last-touched

# Local-only version marker
.local_version

# Export state (local)
export-state/
