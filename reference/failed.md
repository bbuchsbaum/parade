# Select failed jobs

Select failed jobs

## Usage

``` r
failed(x, stage = NULL, ...)
```

## Arguments

- x:

  A `parade_jobset` object

- stage:

  Optional stage filter (ignored for jobsets)

- ...:

  Additional arguments (ignored)
