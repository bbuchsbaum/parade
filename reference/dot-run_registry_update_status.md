# Update run status in the registry

Appends a new entry with updated status (last-writer-wins on read).

## Usage

``` r
.run_registry_update_status(run_id, status, ...)
```

## Arguments

- run_id:

  Character run identifier

- status:

  New status
