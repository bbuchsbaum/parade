# Append a submission failure line to the log

Append a submission failure line to the log

## Usage

``` r
.pipeline_log_submission_failure(
  d,
  path,
  error_msg,
  submitted_jobs,
  expected_jobs
)
```

## Value

NULL, called for side effect of writing the failure.
