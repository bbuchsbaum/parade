# Create a logical type specification

Create a logical type specification

## Usage

``` r
lgl()
```

## Value

A logical vector prototype for schema definitions

## Examples

``` r
schema(is_valid = lgl())
#> # A tibble: 0 × 1
#> # ℹ 1 variable: is_valid <lgl>
```
