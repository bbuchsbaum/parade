% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sink_quick.R
\name{sink_format}
\alias{sink_format}
\title{Define an inline sink format}
\usage{
sink_format(writer, reader = NULL, ext = NULL, atomic = TRUE)
}
\arguments{
\item{writer}{Function or formula for writing}

\item{reader}{Function or formula for reading}

\item{ext}{File extension}

\item{atomic}{Whether writes should be atomic}
}
\value{
List with format definition
}
\description{
Create a format definition inline without registering it globally.
Useful for one-off formats or when you don't want to modify the global registry.
}
\examples{
# Define a custom format inline
my_format <- sink_format(
  writer = ~ jsonlite::write_json(.x, .path, pretty = TRUE),
  reader = ~ jsonlite::read_json(.path),
  ext = ".json"
)

# Use with sink_quick
sink_quick("data", write = my_format$writer, read = my_format$reader)
}
