% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitoring.R
\name{registry_clean}
\alias{registry_clean}
\title{Clean up old jobs from registry}
\usage{
registry_clean(
  registry = NULL,
  older_than = NULL,
  status = NULL,
  dry_run = FALSE
)
}
\arguments{
\item{registry}{Path to registry}

\item{older_than}{Remove jobs older than this (in days)}

\item{status}{Remove jobs with this status (e.g., "COMPLETED", "FAILED")}

\item{dry_run}{If TRUE, show what would be removed without removing}
}
\value{
Number of jobs removed
}
\description{
Remove completed or failed jobs from the registry to save space.
}
\examples{
\donttest{
# Remove completed jobs older than 7 days
registry_clean(older_than = 7, status = "COMPLETED")

# Dry run to see what would be removed
registry_clean(older_than = 30, dry_run = TRUE)
}

}
