% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wtf.R
\name{wtf}
\alias{wtf}
\title{Diagnose pipeline failures}
\usage{
wtf(x, verbose = 2L, max_errors = 20L, log_lines = 15L, ...)
}
\arguments{
\item{x}{A \code{parade_deferred} object, collected results data.frame,
\code{parade_script_job}, or \code{parade_jobset}}

\item{verbose}{Detail level: 0 (summary only), 1 (errors), 2 (full report
with suggestions and logs)}

\item{max_errors}{Maximum number of individual errors to display}

\item{log_lines}{Number of log tail lines to show per failed chunk}

\item{...}{Additional arguments passed to methods}
}
\value{
A \code{parade_failure_report} object (invisibly), printed as side effect
}
\description{
The primary entry point for understanding what went wrong in a parade
pipeline run. Provides classified errors, actionable suggestions,
and log locations.
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
d <- submit(fl)
deferred_await(d, timeout = 60)
wtf(d)
}
}
