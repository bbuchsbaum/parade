% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist_mirai.R
\name{dist_mirai}
\alias{dist_mirai}
\title{Create mirai distribution specification}
\usage{
dist_mirai(
  n = NULL,
  url = NULL,
  remote = NULL,
  dispatcher = TRUE,
  tls = FALSE,
  port = NULL,
  stop_on_exit = TRUE,
  within = c("mirai", "sequential"),
  workers_within = NULL,
  chunks_per_job = 1L,
  by = NULL
)
}
\arguments{
\item{n}{Number of local daemons to launch}

\item{url}{Listening URL for remote connections (auto-generated if NULL)}

\item{remote}{Remote configuration from \code{mirai::ssh_config()} or \code{mirai::cluster_config()}}

\item{dispatcher}{Use dispatcher for automatic load balancing}

\item{tls}{Use TLS encryption for connections}

\item{port}{Port number for connections (auto-selected if NULL)}

\item{stop_on_exit}{Automatically cleanup daemons when finished}

\item{within}{Execution strategy within each job: "mirai" or "sequential"}

\item{workers_within}{Number of workers for nested parallelization}

\item{chunks_per_job}{Number of groups to process per job}

\item{by}{Column names to group by for parallelization}
}
\value{
A \code{parade_dist} object for mirai execution
}
\description{
Configure distributed execution using mirai, supporting local daemons,
SSH tunneling, and SLURM-managed daemon pools. Mirai provides low-latency
task execution without R's connection limits.
}
\examples{
# Local daemons
dist_mirai(n = 4)

# With TLS encryption
dist_mirai(n = 8, tls = TRUE, port = 5555)

# SSH remotes (requires configuration)
\donttest{
dist_mirai(
  remote = quote(mirai::ssh_config(c("ssh://node1", "ssh://node2"))),
  dispatcher = TRUE
)
}
}
