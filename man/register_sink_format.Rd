% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sink_registry.R
\name{register_sink_format}
\alias{register_sink_format}
\title{Register a sink format}
\usage{
register_sink_format(name, writer, reader, ext = NULL, atomic = TRUE)
}
\arguments{
\item{name}{Character string naming the format (e.g., "rds", "csv", "parquet")}

\item{writer}{Function to write data: function(x, path, ...) returning path(s)}

\item{reader}{Function to read data: function(path, ...) returning object}

\item{ext}{Default file extension including dot (e.g., ".rds", ".csv")}

\item{atomic}{Whether writes should be atomic (temp then rename)}
}
\value{
Invisibly returns the format name
}
\description{
Register a format writer/reader pair for use with sinks. Formats can be
referenced by name in sink_spec(), sink_quick(), and other sink functions.
}
\examples{
# Register a custom format
register_sink_format("qs",
  writer = function(x, path, ...) qs::qsave(x, path, ...),
  reader = function(path, ...) qs::qread(path, ...),
  ext = ".qs"
)

# Use in sink
sink_quick("data", write = "qs")
}
