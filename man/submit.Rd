% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deferred.R
\name{submit}
\alias{submit}
\title{Submit a flow for deferred execution}
\usage{
submit(
  fl,
  mode = c("index", "results"),
  run_id = NULL,
  registry_dir = NULL,
  index_dir = NULL,
  seed_furrr = TRUE,
  scheduling = 1
)
}
\arguments{
\item{fl}{A \code{parade_flow} object with distribution settings}

\item{mode}{Execution mode: "index" (default) or "results"}

\item{run_id}{Optional run identifier (auto-generated if NULL)}

\item{registry_dir}{Directory for execution registry}

\item{index_dir}{Directory for result indices}

\item{seed_furrr}{Whether to enable deterministic random number generation}

\item{scheduling}{Furrr scheduling parameter}
}
\value{
A \code{parade_deferred} object for monitoring execution
}
\description{
Submits a parade flow for asynchronous execution, either locally using
future or on SLURM using batchtools. Returns a handle for monitoring
and collecting results.
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))

deferred <- submit(fl)
}
}
