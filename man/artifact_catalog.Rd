% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/artifact_catalog.R
\name{artifact_catalog}
\alias{artifact_catalog}
\title{Artifact catalog utilities}
\usage{
artifact_catalog(
  dir = "artifacts://",
  recursive = TRUE,
  pattern = NULL,
  limit = Inf,
  include_without_sidecar = FALSE
)
}
\arguments{
\item{dir}{Directory (or alias path like \code{"artifacts://"}). Defaults to \code{"artifacts://"}.}

\item{recursive}{Whether to scan subdirectories recursively.}

\item{pattern}{Optional regex to filter artifact \emph{paths}.}

\item{limit}{Maximum number of artifacts to return (after filtering).}

\item{include_without_sidecar}{Whether to include files that lack a sidecar.}
}
\value{
A tibble with one row per artifact.
}
\description{
These helpers make artifacts discoverable by scanning a directory for sink
sidecars (e.g., \verb{*.rds.json}) and combining file stats with stored metadata.

Scans a directory for Parade sink sidecars (\verb{<artifact>.json}) and returns a
tibble that can be filtered/searched.
}
\examples{
\donttest{
paths_init(quiet = TRUE)
artifact_catalog()
}
}
