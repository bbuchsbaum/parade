% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slurm_map.R
\name{element_log}
\alias{element_log}
\title{Read the captured stdio of a packed element}
\usage{
element_log(x, index = NULL, name = NULL, stream = c("err", "out"), n = Inf)
}
\arguments{
\item{x}{A \code{parade_jobset} produced by \code{slurm_map(.packed = TRUE)}.}

\item{index}{Global element index (1-based) to look up. Either \code{index} or
\code{name} must be supplied.}

\item{name}{Element name or stem to look up (e.g. the file stem when
\code{.name_by = "stem"}).}

\item{stream}{One of \code{"err"} or \code{"out"}.}

\item{n}{Maximum number of trailing lines to return (default \code{Inf}).}
}
\value{
Character vector of log lines, or \code{NULL} if the file is missing.
Attribute \code{path} carries the file path that was read.
}
\description{
For a packed jobset launched with \code{slurm_map(.packed = TRUE)} and the
(default) \code{callr} parallel backend, each child worker's stdout and stderr
are redirected to durable per-element files under the registry. This
function locates and reads them so failed runs can be inspected after the
fact without re-running the chunk.
}
