% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deferred.R
\name{deferred_await}
\alias{deferred_await}
\title{Wait for deferred execution to complete}
\usage{
deferred_await(d, timeout = Inf, poll = 10)
}
\arguments{
\item{d}{A \code{parade_deferred} object}

\item{timeout}{Maximum time to wait in seconds}

\item{poll}{Polling interval in seconds}
}
\value{
The input deferred object (invisibly)
}
\description{
Wait for deferred execution to complete
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
deferred <- submit(fl)
deferred_await(deferred, timeout = 600)
}
}
