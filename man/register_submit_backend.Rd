% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submit_backends.R
\name{register_submit_backend}
\alias{register_submit_backend}
\title{Register a submit backend}
\usage{
register_submit_backend(name, submitter, overwrite = FALSE)
}
\arguments{
\item{name}{Backend name.}

\item{submitter}{A function with signature
\verb{function(handle, dist, chunks, index_dir_resolved, mode, seed_furrr, scheduling)}
that returns an updated \code{handle} with \code{handle$jobs} populated.}

\item{overwrite}{Logical; overwrite an existing backend registration.}
}
\value{
Invisibly \code{TRUE}.
}
\description{
\code{submit()} dispatches to a backend by name (e.g., \code{"local"}, \code{"slurm"},
\code{"mirai"}). This function registers a handler so downstream packages can add
new backends (e.g., a future "slurm pool" implementation).
}
\examples{
\dontrun{
register_submit_backend("custom", function(handle, dist, ...) { handle })
}
}
