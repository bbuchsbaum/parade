% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_log.R
\name{parade_watch}
\alias{parade_watch}
\title{Watch a deferred pipeline and log errors incrementally}
\usage{
parade_watch(d, interval = 30, log_path = "parade.log", max_errors = 20L)
}
\arguments{
\item{d}{A \code{parade_deferred} object}

\item{interval}{Polling interval in seconds (default 30)}

\item{log_path}{Path to log file (default "parade.log"). Set to \code{NULL} to
disable file logging (still blocks until done).}

\item{max_errors}{Maximum error lines to write per run (default 20)}
}
\value{
The deferred object (invisibly)
}
\description{
Polls \code{deferred_errors()} and \code{deferred_status()} every \code{interval}
seconds. Only appends \strong{new} errors to the log (deduplicates via in-memory
signature set). Writes a \verb{[DONE]} summary when all chunks complete.
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
d <- submit(fl)
parade_watch(d, interval = 5, log_path = NULL)
}
}
