% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribute.R
\name{dist_slurm_profile}
\alias{dist_slurm_profile}
\title{Convenience: SLURM distribution from a named profile}
\usage{
dist_slurm_profile(
  profile,
  by = NULL,
  within = c("multisession", "multicore", "callr", "sequential"),
  workers_within = NULL,
  template = slurm_template(),
  chunks_per_job = 1L,
  target_jobs = NULL
)
}
\arguments{
\item{profile}{Profile identifier passed to \code{slurm_resources()}. Typically a
character name registered via \code{slurm_defaults_set()} or \code{profile_register()};
can also be a \code{parade_profile} object or a plain list.}

\item{by}{Optional column names to group by for parallelization.}

\item{within}{Execution strategy within each SLURM job: "multisession", "multicore", "callr", or "sequential".}

\item{workers_within}{Number of workers used within each SLURM job.}

\item{template}{Path to SLURM template file.}

\item{chunks_per_job}{Number of groups to process per SLURM job.}

\item{target_jobs}{Optional integer; target number of SLURM jobs to create
(overrides \code{chunks_per_job} at submit time).}
}
\value{
A \code{parade_dist} object suitable for \code{distribute()}
}
\description{
Builds a SLURM distribution spec using a resource profile name (or profile
object) so you don't have to manually call \code{slurm_resources()} in common
cases.
}
\examples{
# Create SLURM distribution using a profile
\dontrun{
dist <- dist_slurm_profile("standard", by = "group")
}

# Use multicore for within-job parallelism
\dontrun{
dist <- dist_slurm_profile("highmem", within = "multicore", workers_within = 16)
}

# Configuration example (no execution)
\dontrun{
flow(grid) |>
  stage("analyze", analyze_fn) |>
  distribute(dist_slurm_profile("standard"))
}
}
