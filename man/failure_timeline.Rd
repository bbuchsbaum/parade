% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/failure_timeline.R
\name{failure_timeline}
\alias{failure_timeline}
\title{Build a chronological timeline of failures}
\usage{
failure_timeline(x, around_failure = 60, ...)
}
\arguments{
\item{x}{A \code{parade_deferred} object or collected results data.frame}

\item{around_failure}{Seconds of context to show around each failure event}

\item{...}{Additional arguments (unused)}
}
\value{
A tibble with columns: \code{offset}, \code{offset_str}, \code{event},
\code{chunk_id}, \code{stage}, \code{context}, \code{class}
}
\description{
Combines \code{.diag} timestamps, SLURM sacct times, and event store data
to produce a chronological view of what happened during a pipeline run.
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
d <- submit(fl)
deferred_await(d, timeout = 60)
tl <- failure_timeline(d)
}
}
