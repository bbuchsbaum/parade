% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script_stage.R
\name{script_returns}
\alias{script_returns}
\title{Declare script output files}
\usage{
script_returns(...)
}
\arguments{
\item{...}{Named arguments where names are output names and values
are file paths. All files must exist at the time of the call.}
}
\value{
Invisibly returns the named list of paths.
}
\description{
Called from within a script executed by \code{\link[=script_stage]{script_stage()}} to declare
which files the script produced. Required when \code{produces} contains
only output \strong{names} (no path templates). The names passed here must
match the names declared in \code{produces}.
}
\details{
Communication uses the \code{PARADE_MANIFEST} environment variable, which
\code{script_stage()} sets automatically before running the script.
}
\examples{
\dontrun{
# In a script called via:
#   script_stage("fit", script = "fit.R", produces = c("model", "metrics"))

saveRDS(my_model, "output/model.rds")
write.csv(metrics_df, "output/metrics.csv")

script_returns(
  model   = "output/model.rds",
  metrics = "output/metrics.csv"
)
}
}
