% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flow.R
\name{add_stage}
\alias{add_stage}
\title{Add a pre-defined stage to a parade flow}
\usage{
add_stage(fl, def)
}
\arguments{
\item{fl}{A \code{parade_flow} object.}

\item{def}{A \code{parade_stage_def} object created by \code{\link[=stage_def]{stage_def()}}.}
}
\value{
The input flow with the stage added.
}
\description{
Inserts a stage created by \code{\link[=stage_def]{stage_def()}} into a flow.
This allows complex stages to be defined once and reused across
multiple flows.
}
\examples{
sq <- stage_def("sq",
  f = function(x) list(result = x^2),
  schema = returns(result = dbl())
)

grid <- data.frame(x = 1:3)
result <- flow(grid) |>
  add_stage(sq) |>
  collect(engine = "sequential")
}
