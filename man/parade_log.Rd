% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_store.R
\name{parade_log}
\alias{parade_log}
\title{Log a message from within a stage function}
\usage{
parade_log(msg, severity = "info", ...)
}
\arguments{
\item{msg}{Character message to log}

\item{severity}{One of \code{"info"} (default), \code{"warn"}, or \code{"error"}}

\item{...}{Additional named fields to include in the event record
(e.g., \code{iteration = 5L}, \code{metric = 0.83})}
}
\value{
\code{NULL} (invisible), called for side effect
}
\description{
Call this inside a stage function to write a structured message to the
run's event log. The message appears in \code{pipeline_top()} event feeds,
\code{failure_timeline()}, and the raw JSONL event store. It never throws
an error -- if logging fails (e.g., outside a pipeline run), the call
is silently ignored.
}
\examples{
\donttest{
fl <- flow(data.frame(x = 1:4)) |>
  stage("compute", function(x) {
    parade_log("starting heavy computation", iteration = x)
    result <- x^2
    if (result > 10) parade_log("large result detected", severity = "warn")
    list(y = result)
  }, schema = returns(y = dbl()))
}
}
