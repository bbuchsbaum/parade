% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_search.R
\name{find_logs}
\alias{find_logs}
\title{Find log files for a pipeline run}
\usage{
find_logs(x, failed_only = TRUE)
}
\arguments{
\item{x}{A \code{parade_deferred} object}

\item{failed_only}{If TRUE (default), only return logs for failed chunks}
}
\value{
A tibble with columns: \code{chunk_id}, \code{batch_id}, \code{state},
\code{log_path}, \code{log_size}, \code{log_lines}
}
\description{
Discovers log files from batchtools registries, SLURM stdout/stderr,
and sacct paths. Returns a tibble with log metadata.
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
d <- submit(fl)
deferred_await(d, timeout = 60)
logs <- find_logs(d)
}
}
