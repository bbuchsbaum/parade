% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deferred.R
\name{parade_run_row}
\alias{parade_run_row}
\title{Run a single grid row through all stages}
\usage{
parade_run_row(
  flow_path,
  chunks_path,
  chunk_id,
  row_id,
  index_dir,
  mode = "index"
)
}
\arguments{
\item{flow_path}{Path to the serialized flow object (RDS).}

\item{chunks_path}{Path to the serialized chunk-index list (RDS).
Currently unused inside the child — kept in the signature so a future
version can validate that \code{row_id} belongs to \code{chunk_id}.}

\item{chunk_id}{Integer; the chunk this row belongs to. Used only for
output-file naming and run-context provenance.}

\item{row_id}{Integer; 1-based row index into \code{fl$grid}.}

\item{index_dir}{Directory where the per-row RDS file is written.}

\item{mode}{Either \code{"index"} or \code{"results"} — matches the semantics of
\code{\link[=parade_run_chunk_local]{parade_run_chunk_local()}}.}
}
\value{
Invisibly \code{NULL}. The per-row RDS file is the actual output.
}
\description{
Entry point used by the \code{within = "parallel"} gnu-parallel backend.
Each row becomes an isolated \code{Rscript} subprocess that calls this
function.  The result is written to
\verb{<index_dir>/chunk-<chunk_id>-row-<row_id>.rds} and the calling parent
reads every per-row RDS back into a tibble after gnu-parallel exits.
}
\keyword{internal}
