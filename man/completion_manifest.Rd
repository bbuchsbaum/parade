% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/completion_manifest.R
\name{completion_manifest}
\alias{completion_manifest}
\title{Read a stage's completion manifest}
\usage{
completion_manifest(stage_id, verify = FALSE, config_dir = NULL)
}
\arguments{
\item{stage_id}{Character stage identifier.}

\item{verify}{Logical; if \code{TRUE}, adds an \code{outputs_current}
column indicating whether all output files still exist on disk.}

\item{config_dir}{Config directory (defaults to \code{paths_get()$config}).}
}
\value{
A tibble with columns \code{stage_id}, \code{param_hash},
\code{params} (list column), \code{output_paths} (list column),
\code{completed_at}, and optionally \code{outputs_current}.
}
\description{
Returns the completion manifest for a stage as a tibble. Each row
represents a previously completed execution with its parameter values
and output paths.
}
\examples{
\dontrun{
# View all completed runs for the "fit" stage
completion_manifest("fit")

# Verify outputs still exist
completion_manifest("fit", verify = TRUE)
}
}
