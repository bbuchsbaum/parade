% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths_validate.R
\name{paths_validate}
\alias{paths_validate}
\title{Validate parade path configuration}
\usage{
paths_validate(
  paths = paths_get(),
  aliases = NULL,
  create = FALSE,
  check_writable = TRUE
)
}
\arguments{
\item{paths}{A named list of path roots, defaulting to \code{\link[=paths_get]{paths_get()}}.}

\item{aliases}{Which aliases to validate. Default validates all configured aliases.}

\item{create}{Whether to create missing directories (except \code{project}, which is never created).}

\item{check_writable}{Whether to check directory writability.}
}
\value{
A list with components:
\itemize{
\item \code{ok}: overall success (no errors)
\item \code{results}: a tibble with per-alias checks
\item \code{warnings}: character vector of advisory warnings
\item \code{errors}: character vector of errors
}
}
\description{
Checks existence and writability of the configured path aliases set by
\code{\link[=paths_init]{paths_init()}}. Optionally creates missing directories.
}
\details{
This is intended as a lightweight "doctor" for first-time setup (especially on
HPC systems) and for catching misconfiguration early (e.g., empty env vars or
node-local scratch).
}
\examples{
paths_init(quiet = TRUE)
paths_validate()
paths_validate(create = TRUE)
}
