% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_store.R
\name{parade_heartbeat}
\alias{parade_heartbeat}
\title{Emit a lightweight heartbeat from within a stage function}
\usage{
parade_heartbeat(msg = NULL, stage = NULL, ...)
}
\arguments{
\item{msg}{Optional short message to include with the heartbeat.}

\item{stage}{Optional stage identifier override. Defaults to the current
stage from the run context when available.}

\item{...}{Additional named fields to include in the event record.}
}
\value{
\code{NULL} (invisible), called for side effect.
}
\description{
Call this from long-running stage code to signal that work is still active
without relying on stdout/stderr log scraping. The heartbeat is written to
the run event log and appears in dashboard/event feeds.
}
\examples{
\donttest{
fl <- flow(data.frame(x = 1:2)) |>
  stage("compute", function(x) {
    parade_heartbeat("starting")
    Sys.sleep(1)
    parade_heartbeat("still working", step = 2L)
    list(y = x^2)
  }, schema = returns(y = dbl()))
}
}
