% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jobset_as_jobset.R
\name{as_jobset}
\alias{as_jobset}
\title{Convert a job or list of jobs to a jobset}
\usage{
as_jobset(x, ...)
}
\arguments{
\item{x}{A parade job object, list of jobs, or existing jobset}

\item{...}{Additional arguments (unused)}
}
\value{
A \code{parade_jobset} object
}
\description{
Converts single job objects or lists of jobs into a \code{parade_jobset},
ensuring uniform behavior across all job types.
}
\examples{
# Local examples (no SLURM required)
job <- slurm_call(function(x) x^2, x = 10, engine = "local")
jobset <- as_jobset(job)

jobs <- slurm_map(1:3, function(x) x^2, .engine = "local")
jobset <- as_jobset(jobs)

\donttest{
# SLURM examples (only run when SLURM is available)
if (Sys.which("squeue") != "") {
  job <- slurm_call(function(x) x^2, x = 10)
  jobset <- as_jobset(job)
}
}

}
