% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/script_stage.R
\name{get_arg}
\alias{get_arg}
\title{Retrieve a command-line argument}
\usage{
get_arg(key, default, type = NULL)
}
\arguments{
\item{key}{Character name (e.g. \code{"x"}) or integer position (e.g. \code{1L}).}

\item{default}{Value to return if the argument is missing. If omitted
and the argument is not found, an error is raised.}

\item{type}{Optional type to coerce to: \code{"character"}, \code{"integer"},
\code{"double"}, \code{"numeric"}, or \code{"logical"}. Overrides auto-coercion.}
}
\value{
The argument value, coerced to the appropriate type.
}
\description{
Lightweight argument parser for scripts run by \code{\link[=script_stage]{script_stage()}}. Supports
named (\code{--key=value}) and positional arguments. Values are auto-coerced
to integer, double, or logical when unambiguous.
}
\details{
When called inside a \code{script_stage()} with \code{engine = "source"}, reads
from injected parameters (via \code{getOption("parade.args")}) so scripts
work identically across engines.
}
\examples{
\dontrun{
# In a script:
x   <- get_arg("x")
n   <- get_arg("n")              # auto-coerced to integer
out <- get_arg("output_path")
v   <- get_arg("verbose", FALSE) # default if missing
}
}
