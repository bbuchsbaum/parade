% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/error_policy.R
\name{retry}
\alias{retry}
\title{Retry failed jobs in a jobset}
\usage{
retry(jobs, policy = NULL, which = "failed")
}
\arguments{
\item{jobs}{A parade_jobset object}

\item{policy}{Error policy to use (or use jobset's policy)}

\item{which}{Which jobs to retry: "failed", "all", or job indices}
}
\value{
Updated jobset with retried jobs
}
\description{
Retry jobs that have failed, respecting the error policy settings.
}
\examples{
\donttest{
# A minimal local example (no SLURM required):
# Create a job that fails once, then succeeds on retry.
env <- new.env(parent = emptyenv())
env$i <- 0L
flaky <- function(x) {
  env$i <- env$i + 1L
  if (env$i == 1L) stop("boom")
  x + 1
}
jobs <- slurm_call(
  flaky,
  x = 1,
  engine = "local",
  .as_jobset = TRUE,
  .error_policy = on_error(action = "continue")
)
jobs <- retry(
  jobs,
  policy = on_error_retry(times = 1, delay = 0, backoff = "none")
)
jobs[[1]]$result
}

}
