% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/name_helpers.R
\name{name_by}
\alias{name_by}
\title{Create a custom naming function}
\usage{
name_by(type = c("stem", "index", "digest", "auto"), ...)
}
\arguments{
\item{type}{Type of naming: "stem", "index", "digest", or "custom"}

\item{...}{Additional arguments passed to the naming function}
}
\value{
A naming function
}
\description{
Helper to create naming functions with common patterns
}
\examples{
\donttest{
if (interactive()) {
  # Equivalent to stem()
  files <- c("data1.csv", "data2.csv")
  process_file <- function(x) x  # stub for example
  jobs <- slurm_map(files, process_file, .name_by = name_by("stem"), .engine = "local")

  # With arguments
  data <- 1:5
  process_data <- function(x) x  # stub for example
  jobs <- slurm_map(
    data,
    process_data,
    .name_by = name_by("index", prefix = "task"),
    .engine = "local"
  )
}
}

}
