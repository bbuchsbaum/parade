% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sink_quick.R
\name{sink_temp}
\alias{sink_temp}
\title{Create a temporary sink specification}
\usage{
sink_temp(
  fields,
  write = "rds",
  read = NULL,
  ext = NULL,
  prefix = "parade-quick",
  template = "{.stage}/{.field}/{.row_key}",
  autoload = FALSE,
  overwrite = c("skip", "overwrite", "error"),
  sidecar = c("json", "none"),
  atomic = TRUE,
  checksum = TRUE,
  ...
)
}
\arguments{
\item{fields}{Character vector of field names to persist}

\item{write}{Format name (e.g., "rds", "csv"), function, or formula for writing}

\item{read}{Optional function or formula for reading}

\item{ext}{File extension including dot}

\item{prefix}{Prefix for temp directory (default: "parade-quick")}

\item{template}{Glue template for file paths}

\item{autoload}{Whether to automatically load artifacts}

\item{overwrite}{Overwrite policy}

\item{sidecar}{Sidecar metadata format}

\item{atomic}{Whether to use atomic writes}

\item{checksum}{Whether to compute checksums}

\item{...}{Additional arguments}
}
\value{
A \code{parade_sink} specification object
}
\description{
Like sink_quick() but writes to a run-scoped temporary directory.
Perfect for testing and ephemeral workflows that don't need permanent storage.
}
\examples{
# Quick temp sink for testing
tmp_sink <- sink_temp("result", write = "rds")

# CSV temp sink
csv_tmp <- sink_temp("data",
  write = ~ write.csv(.x, .path, row.names = FALSE),
  ext = ".csv"
)

# Will write to tempdir()/parade-quick-<runid>/...
}
