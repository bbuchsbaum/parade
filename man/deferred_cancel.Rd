% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deferred.R
\name{deferred_cancel}
\alias{deferred_cancel}
\title{Cancel deferred execution jobs}
\usage{
deferred_cancel(d, which = c("running", "all"))
}
\arguments{
\item{d}{A \code{parade_deferred} object}

\item{which}{Which jobs to cancel: "running" or "all"}
}
\value{
The input deferred object (invisibly)
}
\description{
Cancel deferred execution jobs
}
\examples{
\donttest{
grid <- data.frame(x = 1:4, group = rep(c("A", "B"), 2))
fl <- flow(grid) |>
  stage("calc", function(x) x^2, schema = returns(result = dbl())) |>
  distribute(dist_local(by = "group"))
deferred <- submit(fl)
deferred_cancel(deferred, which = "running")
}
}
